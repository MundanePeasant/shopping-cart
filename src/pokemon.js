//basis for the pokemon objects
function Pokemon(name, imageURL, weight, baseStats) {
  this.name = name;
  this.imageURL = imageURL;
  this.weight = weight;
  this.baseStats = baseStats;
}

export default Pokemon;
